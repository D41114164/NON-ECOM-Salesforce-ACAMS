@isTest(SeeAllData = true)
private class TestSchBatchWebinarsUpsert
{
    
    static testMethod void testExcecute() {
     /*   Test.startTest();
        String CRON_EXP = '0 0 * * * ?';
        schBatchWebinarsUpsert sch = new schBatchWebinarsUpsert();
        system.schedule('Becker LMS - Batch Webinars Update', CRON_EXP, sch);
        Test.stopTest();
*/    
}
}